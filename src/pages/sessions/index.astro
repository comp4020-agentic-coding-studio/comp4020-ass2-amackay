---
import ContentLayout from "astro-theme-university/layouts/ContentLayout.astro";
import SessionsGrid from "../../components/SessionsGrid.astro";
import { sessionLabels, siteConfig } from "../../site-config";
---

<ContentLayout
  title={sessionLabels.plural}
  description={`The twelve-week teaching schedule: what happens in each ${sessionLabels.singular.toLowerCase()}, what to prepare, and what leaves the room.`}
  {...siteConfig}
>
  <p>
    One lab a week, on Wednesdays, in the room with the tutor. Each
    page says what to have done before you arrive, what happens in the room,
    and what you hand in; the list at the end of each is what the lab requires,
    written so you can tell whether you have met it.
  </p>
  <SessionsGrid />
</ContentLayout>
